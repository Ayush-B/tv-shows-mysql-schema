# tv-shows-mysql-schema
MySQL 8 database for modeling TV shows, creators, production companies, networks, and broadcasters. Includes normalized schema in 3NF, junction tables, sample data, and views and triggers that enforce and surface business rules.
